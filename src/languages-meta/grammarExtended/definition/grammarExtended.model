model grammarExtended modifies grammar {

    modify rule_body 
        += enum_rule
        += separated_by_rule
    ;

    enum_rule = {
        members: seq<{ name: string, value: string }>
    };

    separated_by_rule = {
        element: rule_element,
        separator: string,
        min_count: u8
    };

}
